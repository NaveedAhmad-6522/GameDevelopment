### Opening the Project

1. Open Unity Hub.  
2. Click `Add Project` and select the `GD Mini Project 1` folder.  
3. Open the project in Unity Editor.  

Unity will automatically import assets and configure the project settings.
